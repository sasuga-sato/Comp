import styled from "style-components";

export const Users = () => {
  return (
    <SContainer>
      <h2>ユーザー一覧</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
